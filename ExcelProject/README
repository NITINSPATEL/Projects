IMPORTANT FORMULAE:

1. Max/Min
2. IF/IFS
3. Len
4. Left/Right                         // In date , you have to first convert it to TEXT to extract left or right digits
5. Date to Text / TEXT()              // V.Imp. for an analyst  
6. Trim
7. Concatenate
9. Substitute                            // substitute at possibly multiple instances of ith occurence
10. Sum/SumIF/SumIFS
11. Count/CountIF/CountIFS
12. Days/NetworkDays                    // Days give range of days spanned ; NetworkDays give range of working days(removes holidays defined in Excel for the given days range in that calender year, weekends, etc.)
13. XLOOKUP() / VLOOKUP()               // Wildcard entry, not found feature exploration in XLOOKUP() ; SUM(XLOOKUP():XLOOKUP()) -> to sum XLOOKUP in a range ; XLOOKUP() dynamically changes arrays indices if there are modifications , XLOOKUP() is better dure to this


